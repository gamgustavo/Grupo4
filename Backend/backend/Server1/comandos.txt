python -m pip install --upgrade pip
pip install flask
pip install flask_json
pip install sqlalchemy
pip install flask-marshmallow
set FLASK_APP=app.py
set FLASK_ENV=development
flask run

// Todos estos comandos sobre la carpeta server 1
